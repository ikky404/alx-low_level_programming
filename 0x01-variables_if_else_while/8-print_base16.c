#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print numbers of base 16 in lowercase
 *
 * return: Always 0 (success)
 */

int main(void)
{
	int n;
	char l;
	for (n = 0; n<10; n++)
	{
		putchar("%d", n);
	}


	putchar('\n');
	return (0);
}
